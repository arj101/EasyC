int main() {

  int b = 2;
  int a = 2;

  if (a > 5) {
    printf("a is greater than 5");
     b = 10;
  } else {
    printf("a <= 5");
    b = 0;
  }


  for (int i = 0; i < 10; i ++) {
    printf("%d", i);
  }

  char* mem = (char*)malloc(2048);
  char* mem2 = (char*)malloc(1024);
  
  for (int i = 0; i < 30; i++) {
    mem[i] = 'a';
    mem2[i] = 'b';
  }

  free(mem);
  free(mem2);

  return 0;
}
